{
  "name": "@puzzle-book/lander-deploy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npx tsx deploy.ts",
    "deploy:dev": "npx tsx deploy.ts --stage dev",
    "deploy:prod": "npx tsx deploy.ts --stage prod",
    "deploy:fe": "npx tsx deploy.ts --stage dev --frontend-only"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.669.0",
    "@aws-sdk/client-cloudfront": "^3.669.0",
    "@aws-sdk/client-iam": "^3.930.0",
    "@aws-sdk/client-s3": "^3.669.0",
    "@aws-sdk/client-sts": "^3.669.0",
    "chalk": "^5.3.0",
    "dotenv": "^17.2.3",
    "mime-types": "^2.1.35"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20",
    "tsx": "^4.20.6",
    "typescript": "^5.5.4"
  }
}
